#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
	int com, user, win;
	srand(time(NULL));
	com = rand() % 3 + 1;
	
	printf("주먹:1, 가위:2, 보:3 : ");
	scanf("%d", &user);
	
	if(com == 1) {
		printf("")
	}
	
}